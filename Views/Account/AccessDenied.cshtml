@{
    ViewData["Title"] = "Erişim Reddedildi";
}

<div class="max-w-xl mx-auto mt-12 bg-surface-container p-8 rounded-xl border border-white/10 text-center">
    <h1 class="text-2xl font-bold text-error mb-3">Erişim Reddedildi</h1>
    <p class="text-on-surface-variant mb-6">Bu sayfayı görüntülemek için gerekli yetkiniz bulunmuyor.</p>
    <a asp-controller="Home" asp-action="Index" class="inline-flex bg-primary-container text-on-primary px-5 py-2.5 rounded-lg font-semibold">
        Ana Sayfaya Dön
    </a>
</div>
